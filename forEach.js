let numbers = [1, 2, 3, 4, 5]
numbers.forEach((x, index) => {
    console.log(x, index);
})


let fruits = ['Apple', 'Banana', 'Cherry'];
fruits.forEach((fruit) => {
  console.log(fruit);
});


let animals = ['Lion', 'Elephant', 'Zebra'];
animals.forEach((animal, index) => {
  console.log(`${index}: ${animal}`);
});

